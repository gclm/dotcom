interface:
  display_name: "Dotcom"
  short_description: "Repo-specific patterns and workflows for dotcom"
  default_prompt: "Use the dotcom repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true